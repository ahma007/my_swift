import UIKit

//Второе домашнее задание - Коллекции: Arrays, Sets, Dictionaries

//имена друзей (Arrays)
var friends = ["Pete", "Bob", "Charlie", "Alice", "Liam"]
friends.append(contentsOf: ["Sam", "John"]) //добавляем 2 новых друзей (также можно- friends += ["Sam", "John"]
friends.removeLast()        // удаляем последний элемент ("John")
print(friends[2])  // "Charlie"
if friends.contains("Bob") {
    print("Bob есть в списке!")
} else {
    print("Bob не в списке!")
}
print(friends)

//множества (Sets)
var numbers: Set = [4, 8, 15, 16, 23] //множество из пяти различных чисел
numbers.insert(42)          //добавление еще одного числа в это множество
numbers.remove(4)          // удаление одного числа из этого множества
print(numbers)
print(numbers.contains(7)) // false , есть ли число 7 в нашем множестве.

//Снова друзья (Dictionaries)
var favoriteColors = [
    "Pete": "Синий",
    "Bob": "Зелёный",
    "Charlie": "Розовый",
    "Alice": "Красный",
    "Sam": "Фиолетовый"
] //словарь, где ключи - это имена ваших друзей, а значения - их любимый цвет

if var color = favoriteColors["Bob"] {
    print("Любимый цвет Bob — \(color)")
} //любимый цвет Bob

favoriteColors["Alice"] = "Зелёный"  //изменение значения любимыого цвета Alice на зеленый
favoriteColors.removeValue(forKey: "Charlie") //удаляем данные Charlie из словаря

if favoriteColors.keys.contains("Sam") {
    print("Sam есть в списке.")
}

print(favoriteColors)

//словарь с ключами-строками и значениями-integers
//Создаём словарь: ключ — строка, значение — число
var scores: [String: Int] = [
    "Charlie": 4,
    "Bob": 8,
    "Pete": 15,
    "Alice": 16,
    "Liam": 23,
    "Sam": 42
]

//Перебираем словарь с помощью цикла for-in
for (name, score) in scores {
    print("\(name) получил(а) \(score) игровой номер.")
}

//множество из пяти фруктов, добавление еще одного и вывод количества фруктов в множестве
// Создаём множество из пяти фруктов
var fruits: Set = ["Яблоко", "Банан", "Апельсин", "Киви", "Груша"]

// Добавляем ещё один фрукт
fruits.insert("Манго")

// Выводим количество фруктов в множестве
print("Количество фруктов: \(fruits.count)")

//словарь, где ключи это идентификационные номера студентов, а значения - это их имена, а далее выводим имястудента с определенным идентификационным номером.
// Создаём словарь: ключ — ID студента, значение — его имя
let students: [Int: String] = [
    101: "Charlie",
    102: "Bob",
    103: "Pete",
    104: "Alice",
    105: "Liam",
    106: "Sam"
]

// Идентификационный номер, который хотим найти
let searchID = 103

// Проверяем и выводим имя студента по ID
if let name = students[searchID] {
    print("Студент с ID \(searchID): \(name)")
} else {
    print("Студент с ID \(searchID) не найден.")
}

//cловарь с ключами-строками и значениями-integers c использованием цикла for-in для итерации по словарю и печати каждого значения
//Создаём массив из пяти различных чисел
var numerics = [42, 8, 15, 23, 16]

// Сортируем массив по возрастанию
let sortedNumerics = numerics.sorted()

// Выводим результат
print("Отсортированный массив: \(sortedNumerics)")

//множество с названиями всех европейских стран и проверкой является ли Франция частью этого множества
// Создаём множество с именами европейских стран
let europeanCountries: Set = [
    "Албания", "Австрия", "Бельгия", "Болгария", "Хорватия",
    "Чехия", "Дания", "Эстония", "Финляндия", "Франция",
    "Германия", "Греция", "Венгрия", "Ирландия", "Италия",
    "Латвия", "Литва", "Люксембург", "Мальта", "Нидерланды",
    "Польша", "Португалия", "Румыния", "Словакия", "Словения",
    "Испания", "Швеция", "Кипр", "Исландия", "Норвегия", "Андорра", "Швейцария", "Лихтенштейн", "Чехия",
    "Монако", "Сербия", "Великобритания", "Ватикан", "Сан-Марино", "Эстония", "Молдова", "Беларусь", "Украина",
    "Россия", "Азербайлджан", "Грузия", "Армения", "Казахстан", "Северная Македония", "Израиль",
    "Босния и Герцеговина", "Черногория"
]

// Проверяем, есть ли Франция в множестве
if europeanCountries.contains("Франция") {
    print("Франция есть в списке европейских стран.")
} else {
    print("Франции в списке нет.")
}

//словарь, где ключи - это названия городов, а значения - это количество населения этих городов. Затем изменяем население одного из городов.
// Создаём словарь: ключ — город, значение — население
var citiesPopulation: [String: Int] = [
    "Токио": 13960000,
    "Йокогама": 3740000,
    "Осака": 2715000,
    "Нагоя": 2296000,
    "Саппоро": 1952000
]

// Изменяем население города Осака
citiesPopulation["Осака"] = 2750000

// Выводим обновлённый словарь
for (city, population) in citiesPopulation {
    print("\(city): \(population) жителей")
}
